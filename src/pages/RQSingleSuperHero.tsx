import { useParams } from "react-router-dom";

//RQSingleSuperHero
export default function RQSingleSuperHero() {
  // declare and define variables and state
  const { id } = useParams();
  // declare and define component methods
  // return component ui
  return <>RQSingleSuperHero</>;
}
